
#pragma once

#include <list>
#include <random>
#include <string>
#include <functional>

// #define DEBUG true
#define SYS_CONFIG "Setting_game"

static int MAIN_X, MAIN_Y;

std::string test(std::string name_config); 

// template <typename T> string type(T type_data);
// bool comparingPathWithFile(string name_path, string name_file);
